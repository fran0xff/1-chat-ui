# Base URL of the Spring Boot chat backend (REST history endpoint).
VITE_API_BASE_URL=http://localhost:8080

# Base URL of the same backend for the WebSocket broker connection.
VITE_WS_BASE_URL=ws://localhost:8080
